// Represents a subject record returned from the SubjectsController API      // File level summary comment
export interface Subject {
  subjectId: number;                                                        // Unique identifier of the subject
  subjectName: string;                                                      // Display name of the subject
}
