// Holds environment specific configuration values                          // File level summary comment
export const environment = {
  production: false,                                                       // Flags this as the non production environment
  apiBaseUrl: 'https://localhost:7050/api'                                 // Base URL of the ExamManagementAPI backend
};
